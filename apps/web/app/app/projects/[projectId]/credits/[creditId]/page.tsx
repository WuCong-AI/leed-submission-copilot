import { EvidenceCitation, RiskBadge, WorkspaceNav } from "../../../../../../components/workspace";
export default async function CreditWorkspace({ params }: { params: Promise<{ projectId: string; creditId: string }> }) { const { projectId, creditId } = await params; return <main className="shell"><WorkspaceNav projectId={projectId}/><h1>Credit workspace · {creditId}</h1><p>Overview · Requirements · Evidence · Phase Review · Precedents · Tender Requirements · Submission Packet · Comment Risk</p><RiskBadge/><EvidenceCitation file="Registry + uploaded evidence citations"/></main>; }
