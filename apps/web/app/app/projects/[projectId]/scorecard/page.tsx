import { CreditStatusBadge, MissingInformationChecklist, WorkspaceNav } from "../../../../../components/workspace";
export default async function Scorecard({ params }: { params: Promise<{ projectId: string }> }) { const { projectId } = await params; return <main className="shell"><WorkspaceNav projectId={projectId}/><h1>Registry-loaded scorecard</h1><div className="card"><b>Prerequisites first</b><p>Possible/target/estimated/awarded points are API fields. Unknown official options remain blank.</p><CreditStatusBadge status="not_started" /></div><MissingInformationChecklist/></main>; }
