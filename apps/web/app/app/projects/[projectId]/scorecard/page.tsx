"use client";
import { useEffect, useState } from "react";
import { WorkspaceNav } from "../../../../../components/workspace";
import { api } from "../../../../../lib/api";
type Row = { credit: { credit_id: string; credit_name: string; category: string; max_points: number | null; is_prerequisite: boolean }; status: { status: string; estimated_points?: number } };
export default function Scorecard({ params }: { params: Promise<{ projectId: string }> }) { const [id, setId] = useState(""); const [rows, setRows] = useState<Row[]>([]); const [analysis, setAnalysis] = useState<{ target_score: number; certification: string } | null>(null); useEffect(() => { params.then(({ projectId }) => { setId(projectId); api<Row[]>(`/api/projects/${projectId}/scorecard`).then(setRows); api<typeof analysis>(`/api/projects/${projectId}/analysis`).then(setAnalysis).catch(() => undefined); }); }, [params]); return <main className="shell"><WorkspaceNav projectId={id}/><h1>LEED scorecard</h1>{analysis && <div className="score-hero"><div><span className="eyebrow">Automated projection</span><h2>{analysis.certification}</h2></div><strong>{analysis.target_score}</strong></div>}<div className="table-wrap"><table><thead><tr><th>Credit</th><th>Category</th><th>Prerequisite</th><th>Max points</th><th>Status</th></tr></thead><tbody>{rows.map((row) => <tr key={row.credit.credit_id}><td>{row.credit.credit_id} · {row.credit.credit_name}</td><td>{row.credit.category}</td><td>{row.credit.is_prerequisite ? "Yes" : "No"}</td><td>{row.credit.max_points ?? "—"}</td><td><span className="tag">{row.status.status}</span></td></tr>)}</tbody></table></div></main>; }
